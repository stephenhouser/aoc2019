# Day 9: Sensor Boost

Intcode

## Part 1

Add relative mode parameters (mode 2). Rewrite the load/store to handle
this better. Add `base` opcode to adjust the base register. Also add a `debug`
flag to the CPU.

## Part 2

Not much to do here, adjust the input parameter and run.